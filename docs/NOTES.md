# Notes

- Lessons and practice content should be written in **Arabic** (matches real exam, builds reading speed). Meta-commentary/teaching guidance from me can be in either language depending on what the user responds well to — default to mirroring whatever language the user writes in during our conversation.
- Timeline is tight (~1 month, 9+ hrs/week available). Bias toward technique-heavy, high-yield topics first (e.g. quantitative comparisons) rather than a slow ground-up rebuild of math the user likely already has from recent high school.
- No diagnostic test was taken before starting. Treat early lesson performance as the real diagnostic — watch closely for signs the "fresh from high school" assumption in MISSION.md is wrong, and write a learning record + revise pacing if so.
- Target: engineering program admission, needs 95%+.
