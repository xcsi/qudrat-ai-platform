# Screenshots

Add product screenshots here before publishing, then reference them from the
root `README.md`. Suggested shots:

- `dashboard.png` — student dashboard (streak, XP, weak/strong skills)
- `lesson.png` — a rendered lesson (concept block + worked example)
- `companion.png` — the companion (قِيس) in an active mount state
- `diagnostic.png` — diagnostic assessment in progress
- `mock-exam.png` — mock exam / timed simulation screen

Keep images reasonably sized (compress PNGs) so the repo stays lightweight.
